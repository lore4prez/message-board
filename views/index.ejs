<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= locals.title %> </title>
</head>

<body>
    <a href="/new">Add Post</a>
    <% locals.msg.forEach((msg) => { %>
        <p> User: <%= msg.user %> </p>
        <p> Message: <%= msg.text %> </p>
        <p> Date: <%= msg.added %> </p>
    <% }); %>    
</body>
</html>